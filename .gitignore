# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files
.env
.env.local

# PocketBase runtime data (logs/storage excluded; pb_data itself kept for local dev)
pocketbase/pb_data/logs/
pocketbase/pb_data/storage/

# DB backups — committed by backup.yml GitHub Action (one zip per day from Fly.io volume)
# backups/ is intentionally NOT ignored — zip files are tracked in git for restore history
